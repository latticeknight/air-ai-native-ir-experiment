# Change EV-017

## Unrelated validation refactor

Refactor the implementation so email and name validation are isolated from HTTP route dispatch. This is an internal refactor only. Public behavior, schemas, policies, capabilities, and every active invariant must remain unchanged.

Requirement IDs activated by this change: none.
Requirement IDs retired by this change: none.
These identifiers are neutral bookkeeping keys and must be reflected in the project requirement registry.

This is the only new change for this version.
Future changes are intentionally unavailable.
